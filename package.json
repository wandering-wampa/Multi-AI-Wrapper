{
  "name": "multi-ai-cockpit",
  "version": "1.0.0",
  "description": "Local Electron wrapper for ChatGPT, Claude, Copilot, Gemini, and Perplexity in one UI",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . MultiAICockpit --platform=win32 --arch=x64 --out=dist --overwrite",
    "package-mac": "electron-packager . MultiAICockpit --platform=darwin --arch=universal --out=dist --overwrite"
  },
  "devDependencies": {
    "electron": "^34.0.0",
    "electron-packager": "^17.1.2"
  }
}
